要求：
用户名、密码、重复密码不能为空
密码、重复密码长度 6-12位，不能出现空格
密码和重复密码必须一致
layui自定义验证规则使用步骤：
1、加载form模块 （layui封装的JS对象，所有模块，使用前必须先加载，除了layer弹层模块）
2、调用form.verify()编写验证规则
键(验证规则)：值(验证的方法，可使用函数、数组)
数组：['正则表达式','验证失败时的提示信息']
函数：函数名：function(val){
        形参 val，表示使用验证规则的输入框的值
        比如重复密码使用了这个验证规则，形参val表示输入的重复密码
        功能代码
        获取密码	
}